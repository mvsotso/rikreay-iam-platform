apiVersion: v1
kind: Service
metadata:
  name: iam-monitoring-service
  labels:
    app: iam-monitoring-service
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
      name: http
  selector:
    app: iam-monitoring-service
