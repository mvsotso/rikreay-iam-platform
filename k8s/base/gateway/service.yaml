apiVersion: v1
kind: Service
metadata:
  name: iam-gateway
  labels:
    app: iam-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: iam-gateway
