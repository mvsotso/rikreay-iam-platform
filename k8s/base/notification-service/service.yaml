apiVersion: v1
kind: Service
metadata:
  name: iam-notification-service
  labels:
    app: iam-notification-service
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
  selector:
    app: iam-notification-service
