apiVersion: v1
kind: Service
metadata:
  name: iam-config-service
  labels:
    app: iam-config-service
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
      name: http
  selector:
    app: iam-config-service
