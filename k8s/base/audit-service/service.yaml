apiVersion: v1
kind: Service
metadata:
  name: iam-audit-service
  labels:
    app: iam-audit-service
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: iam-audit-service
