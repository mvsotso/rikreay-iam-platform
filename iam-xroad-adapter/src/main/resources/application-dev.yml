spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/iam_xroad
    username: iam_admin
    password: iam_secret_2026
  kafka:
    bootstrap-servers: localhost:9092
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/iam-platform
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_secret_2026

xroad:
  routing:
    core-service-url: http://localhost:8082

logging:
  level:
    com.iam.platform: DEBUG
    org.springframework.security: DEBUG
